<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/list_item"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:minHeight="@dimen/list_item_height"
    tools:background="@color/category_numbers"
    >
    <ImageView
        android:id="@+id/imag_translation"
        android:layout_width="@dimen/list_item_height"
        android:layout_height="@dimen/list_item_height"
        android:src="@drawable/number_one"
        android:background="@color/tan_background"
        android:contentDescription="image for translation" />

    <LinearLayout
        android:id="@+id/text_Container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_toRightOf="@+id/imag_translation"
        android:padding="16dp"
        android:minHeight="@dimen/list_item_height">

        <TextView
            android:id="@+id/tv_miwok_translation"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:gravity="bottom"
            style="@style/TextStyle"
            android:textStyle="bold"
            tools:text="lucci"
            />
        <TextView
            android:id="@+id/tv_default_translation"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:gravity="top"
            style="@style/TextStyle"
            tools:text="one"
            />
    </LinearLayout>

    <ImageView
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:layout_marginRight="16dp"
        android:src="@drawable/ic_play_arrow_white" />

</RelativeLayout>